Place favicon files here:

- `favicon.svg`
- `favicon.ico`
- `apple-touch-icon.png`

The app and fixture pages already reference these files as:

- `./favicon/favicon.svg`
- `./favicon/favicon.ico`
- `./favicon/apple-touch-icon.png`

This folder is served by the local preview server and copied into `dist-static/`
during `npm run build:static`.
